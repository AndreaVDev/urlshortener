[project]
name = "urlshortener"
version = "0.1.0"
description = "Python project for a simple url shortener"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pymongo>=4.9",
]
